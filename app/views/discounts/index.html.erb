<p id="notice"><%= notice %></p>

<h1>Discounts</h1>

<table>
  <thead>
    <tr>
      <th>Guid</th>
      <th>Discount type</th>
      <th>Month number</th>
      <th>Amount</th>
      <th>Minimum amount</th>
      <th>Maximum amount</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @discounts.each do |discount| %>
      <tr>
        <td><%= discount.guid %></td>
        <td><%= discount.discount_type %></td>
        <td><%= discount.month_number %></td>
        <td><%= discount.amount %></td>
        <td><%= discount.minimum_amount %></td>
        <td><%= discount.maximum_amount %></td>
        <td><%= link_to 'Show', discount %></td>
        <td><%= link_to 'Edit', edit_discount_path(discount) %></td>
        <td><%= link_to 'Destroy', discount, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Discount', new_discount_path %>
