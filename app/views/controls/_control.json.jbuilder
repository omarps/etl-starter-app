json.extract! control, :id, :guid, :kind, :min_value, :max_value, :deleted, :deleted_on, :requirements_text, :applicable_discount_plans_count, :unit_type_id, :created_at, :updated_at
json.url control_url(control, format: :json)
