<%= form_with(model: client_application, local: true) do |form| %>
  <% if client_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(client_application.errors.count, "error") %> prohibited this client_application from being saved:</h2>

      <ul>
      <% client_application.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :guid %>
    <%= form.text_field :guid %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :channel_rates_on %>
    <%= form.check_box :channel_rates_on %>
  </div>

  <div class="field">
    <%= form.label :internal %>
    <%= form.check_box :internal %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
